export enum EditorPanels {
  templates = "templates",
  styling = "styling",
  preferences = "preferences",
  wording = "wording",
  output = "output",
}

export enum OutputFormats {
  html = "html",
  json = "json",
}
